name: Hello World
on: workflow_dispatch
jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Hello Step
        run: |
         echo "This is line one"
         echo "Hello, ${{ vars.MY_NAME }}"
         echo "This is docker password, ${{ secrets.DOCKER_PASSWORD }}"
      
